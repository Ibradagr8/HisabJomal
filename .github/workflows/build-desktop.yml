name: Build Windows app

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  bundle-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Run automated tests and build the web interface
        run: npm run check
      - name: Install NSIS for the EXE installer
        run: choco install nsis -y --no-progress
      - run: npm run tauri:build:windows
      - uses: actions/upload-artifact@v7
        with:
          name: atlas-al-huruf-windows-exe
          path: src-tauri/target/**/bundle/nsis/*.exe
      - name: Publish Windows EXE installer to GitHub Releases
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: src-tauri/target/**/bundle/nsis/*.exe
