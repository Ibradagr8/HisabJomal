[package]
name = "hisab-jomal"
version = "0.1.5"
description = "حساب الجمل"
authors = ["Ibrahim bin Salah"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[lib]
name = "hisab_jomal_lib"
crate-type = ["staticlib", "cdylib", "rlib"]
